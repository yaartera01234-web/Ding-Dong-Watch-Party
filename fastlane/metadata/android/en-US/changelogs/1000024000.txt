New look: the whole app was redrawn, with a home form, a room rail and a settings console.

Players: KitePlayer, a new experimental engine, joins ExoPlayer and mpv. VLC on Android is gone.

Sync: the phantom pause is fixed, track picks survive a reload, the room shows when it waits for video.

Room: chat colours follow the theme, mute someone, slash commands, invite links.

Connection: TLS checks the server's certificate; reconnects back off.

Switch the app's language in settings, no restart.
